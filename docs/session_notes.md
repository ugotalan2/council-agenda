Session — May 15, 2026
Completed full deployment of the Council Agenda app. Logged into Azure CLI and ACR, built the Docker image using a native dotnet publish + simple Dockerfile approach (workaround for M4 ARM → amd64 emulation being prohibitively slow). Pushed image to ACR and wired App Service to pull from it using admin credentials. Upgraded App Service from F1 to B1 to resolve quota exhaustion from repeated failed pull attempts. Fixed Swagger being disabled in production, added Vercel origin to CORS policy, and added missing Clerk__Domain env var which was causing all API calls to return 401. Ran EF Core migrations against Supabase using the session pooler connection string. Set up Vercel deployment for the React client. Wired up full GitHub Actions CD pipeline — Vercel auto-deploys on push, Azure uses Dockerfile.multistage for the build step in CI while local dev uses the native publish + simple Dockerfile. All six GitHub secrets added. Both pipelines verified working on first successful run.

Session — May 18, 2026
Built Google Doc export via OAuth2 per-org refresh token flow, replacing failed service account approach (personal Gmail storage quota issues). Set up Google Cloud project under ugotalan account with OAuth2 client. Implemented OrgPositions/MemberPositions models with seeded defaults per org type. Refactored rotation system from member-based to position-based. Built AgendaAttendeesController and fully wired AttendeesSection.tsx with standing members, attendance toggle, guest invites, rotation assignment generation with last-assigned history. Fixed rotation bugs: opening/closing prayer conflict prevention, handbook training cross-org exclusion, meeting-date-aware rotation logs, rotation history excludes current meeting. Added meeting delete with rotation log cascade cleanup. Added IgnoreCycles JSON serializer fix globally. Applied SQL seeds for existing orgs. Supabase migrations pending.
Post-session fixes: Deployed to production. Fixed Vercel client-side routing with vercel.json rewrite rule. Fixed Supabase connection timeout on Azure by adding SSL Mode, Trust Server Certificate, Maximum Pool Size, Connection Idle Lifetime, and Keepalive to connection string. Fixed Google Doc export timezone issue — Azure server runs UTC so switched from ToLocalTime() to explicit America/Chicago timezone conversion. Set GoogleDriveFolderId on production orgs in Supabase. Google OAuth connect and export working in production.